# Bỏ qua thư mục chứa các thư viện đã cài
node_modules/

# Bỏ file lock (chọn một tùy theo bạn dùng npm, yarn hay pnpm)
package-lock.json
yarn.lock
pnpm-lock.yaml

# File môi trường chứa biến nhạy cảm như DB, API key...
.env
.env.* 

# Thư mục build (nếu dùng TypeScript hoặc công cụ build)
dist/
build/

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS system files
.DS_Store
Thumbs.db

# VSCode settings (nếu không muốn chia sẻ config cá nhân)
.vscode/

# Coverage khi chạy test
coverage/

# Tạm thời khi deploy hoặc cấu hình docker
*.tgz
*.log
*.bak

# pm2 logs hoặc logs custom
logs/
*.log

# Nếu dùng TypeScript
*.tsbuildinfo

# Optional: nếu bạn dùng Swagger để gen docs
swagger.json
swagger.yaml
